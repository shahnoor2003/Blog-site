 /*function dark(){
    document.querySelector('.light').classList.toggle('dark');
    document.querySelector('#pic').classList.toggle('pic');
    document.querySelector('#pic2').classList.toggle('pic');
   document.querySelector('body').classList.toggle('bg-dark');
    const hr = document.querySelectorAll('#hr');
      // Convert NodeList to an array
      const hrr = Array.from(hr);
     hrr.forEach((hr) => {
      hr.classList.toggle('hr');
    });
    const item = document.querySelectorAll('a,h4,p,h3');
      // Convert NodeList to an array
      const everytext = Array.from(item);
     everytext.forEach((item) => {
      item.classList.toggle('text-white');
    });
}*/
function Submit(){
  alert("Thanks for your feedback!");
}